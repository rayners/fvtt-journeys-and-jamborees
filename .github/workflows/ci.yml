name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    strategy:
      matrix:
        node-version: [24]

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Run CI Pipeline
        uses: rayners/foundry-module-actions/ci@v5
        with:
          node-version: ${{ matrix.node-version }}
          github-packages: false
          codecov: ${{ matrix.node-version == '24' }}
          codecov-slug: rayners/fvtt-journeys-and-jamborees
          coverage-node-version: '24'
          codecov-token: ${{ secrets.CODECOV_TOKEN }}
          junit-output: ${{ matrix.node-version == '24' }}
          test-command: 'npm run test:coverage:junit'
